
model {
  for (i in 1:N){
    nSpecies[i] ~ dpois(lam.hat[i])
    log(lam.hat[i]) <- beta0 + beta1 * nBurns[i] + beta2 * (nBurns[i])^2
  }
  beta0 ~ dnorm(0, .0001)
  beta1 ~ dnorm(0, .0001)
  beta2 ~ dnorm(0, .0001)
}

