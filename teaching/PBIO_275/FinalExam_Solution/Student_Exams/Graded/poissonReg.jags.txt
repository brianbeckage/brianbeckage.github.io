
model {
  for (i in 1:N){
    sr[i] ~ dpois(lam.hat[i])
    log(lam.hat[i]) <- beta0 + (beta1 * burns[i]) + (beta2 * burns[1])
  }
  beta0 ~ dnorm(0, .0001)
  beta1 ~ dnorm(0, .0001)
  beta2 ~ dnorm(0, .0001)
}

